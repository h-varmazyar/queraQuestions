package main

import (
	"bufio"
	"fmt"
	"os"
	"strconv"
	"strings"
)

package main

import (
"bufio"
"fmt"
"os"
"strconv"
"strings"
)

func main() {
	reader := bufio.NewReader(os.Stdin)
	input, _ := reader.ReadString('\n')

	length, _ := strconv.Atoi(strings.TrimSpace(strings.Split(input, " ")[0]))
	num1, _ := strconv.Atoi(strings.TrimSpace(strings.Split(input, " ")[0]))
	length2, _ := strconv.Atoi(strings.TrimSpace(strings.Split(input, " ")[0]))



}

func calculate() {
	left()
	right()
}

func left() {

}

func right() {

}
